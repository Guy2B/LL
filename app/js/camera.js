window.LuneCamera=(function(){let videoEl,captureCallback;async function init(o){videoEl=document.getElementById('cameraVideo');captureCallback=o?.onCapture||function(){};document.getElementById('btnStartCam').addEventListener('click',startCamera);document.getElementById('btnCapture').addEventListener('click',captureFrame);document.getElementById('uploadInput').addEventListener('change',handleUpload);}async function startCamera(){try{const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:'user'},audio:false});videoEl.srcObject=s;await videoEl.play();}catch(e){console.warn('Camera denied',e);document.getElementById('uploadInput').click();}}function captureFrame(){if(!videoEl)return;const size=Math.min(videoEl.videoWidth,videoEl.videoHeight)||640;const c=document.createElement('canvas');c.width=size;c.height=size;const ctx=c.getContext('2d');ctx.drawImage(videoEl,(videoEl.videoWidth-size)/2||0,(videoEl.videoHeight-size)/2||0,size,size,0,0,size,size);const dataURL=c.toDataURL('image/jpeg',0.95);captureCallback(dataURL);}function handleUpload(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>captureCallback(r.result);r.readAsDataURL(f);}return{init,startCamera,captureFrame};})();